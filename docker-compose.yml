services:
  discord:
    build: ./
    restart: always
    container_name: ui
    environment:
      - TOKEN=${TOKEN}
      - STEAM_TOKEN=${STEAM_TOKEN}
    volumes:
      - ui_db:/usr/src/app/data

volumes:
  ui_db: